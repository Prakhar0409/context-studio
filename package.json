{
  "name": "context-studio",
  "private": true,
  "version": "0.1.0",
  "description": "Visualize and (later) prune an LLM coding-agent's context window. Reads Claude Code session transcripts.",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/server & npm run dev --workspace=packages/web",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/server && npm run build --workspace=packages/web",
    "typecheck": "tsc -b packages/shared packages/server"
  }
}
